apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-server-headless
  namespace: {{ .Values.namespace }}
spec:
  clusterIP: None
  selector:
    app: {{ .Release.Name }}-server
  ports:
    - name: http
      port: 9090
      targetPort: http
